# Core dependencies (Python 3.11 required)
mutmut>=2.0.0
pytest>=7.0.0
python-dotenv>=0.19.0

# Test dependencies
numpy>=1.21.0
boto3>=1.20.0
botocore>=1.23.0

# MutaHunter (LLM-powered mutations)
# Installed from GitHub (not PyPI) to get latest version
# Install with: ./venv/bin/pip install git+https://github.com/codeintegrity-ai/mutahunter.git
# Note: Bug fix required - see setup instructions in README.md
