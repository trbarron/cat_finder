# Core dependencies for agentic_testing module
mutmut>=2.0.0
pytest>=7.0.0

# Optional: MutaHunter (LLM-powered mutations)
# Uncomment to use mutahunter:
# mutahunter>=0.1.0
# certifi>=2023.0.0  # Required by mutahunter
# httpx>=0.24.0      # Required by mutahunter
# litellm>=1.0.0     # Required by mutahunter

# Python standard library (no install needed):
# - json
# - os
# - re
# - subprocess
# - pathlib
# - urllib.request
# - sys
# - argparse

# Environment variables required:
# - OPENAI_API_KEY: API key for OpenAI (or compatible LLM service)

# Optional (for running with the test suite):
# - unittest.mock (standard library)
# - numpy (if your code uses it)
# - boto3 (if your code uses AWS)
# - Any other project-specific dependencies
